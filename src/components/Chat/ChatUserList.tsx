import React from "react";
import Box from "@mui/material/Box";

const ChatUserList: React.FC<{ name?: string; message?: string }> = (
    props,
    context
) => {
    return (<Box>
        <>
            123
        </>
    </Box>)
};

export default ChatUserList;
